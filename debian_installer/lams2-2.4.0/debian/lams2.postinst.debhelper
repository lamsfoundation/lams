# Automatically added by dh_installinit
if [ -x "/etc/init.d/lams2" ]; then
	update-rc.d lams2 defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lams2 start || exit $?
	else
		/etc/init.d/lams2 start || exit $?
	fi
fi
# End automatically added section
