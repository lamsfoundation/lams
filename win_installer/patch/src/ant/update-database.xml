<?xml version="1.0" encoding="UTF-8"?>
<project name="update-database" >
	<!-- begin properties *******************************************************************************************-->
	<!-- import properties from the specified file -->
	
	<property file="core.properties"/>
	
	<target name="update-database"  description="Runs some update scripts found in lams_common\db\sql\updatscripts" >
		<sql driver="com.mysql.jdbc.Driver" 
			classpath="${jboss.deploy}/mysql-connector-java-5.0.8-bin.jar" 
			url="${db.url}" 
			userid="${db.username}" 
			password="${db.password}"
			encoding="utf8"
			onerror="abort">
			<transaction src="${temp}/lams/sql/update-script.sql" />
		</sql>
	</target>
</project>