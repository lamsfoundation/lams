LAMS Signup

Tested with LAMS 2.3.3 and LAMS 2.3.4.  Shouldn't require too much if any 
modification to work with 2.4.

Installation
------------
Run deploy-war task from build.xml Ant file.
This will deploy both jar and war to lams.ear
and make necessary modifications to JBoss configuration file.

Otherwise follow manual installation:

Drop jar and war into lams.ear.  Modify lams.ear/META-INF/application.xml and
add the following;

<module>
        <web>
            <web-uri>lams-signup.war</web-uri>
            <context-root>/lams/signup</context-root>
                </web>
    </module>
    <module>
        <java>lams-signup.jar</java>
    </module>
    
TODO
----
- signup stats (already stored in SignupUser)
- full i18n
- maintain pretty urls between POSTs