<?xml version="1.0" encoding="UTF-8"?>
<project name="filter-config" basedir="..">
	<property file="lams.properties" />
	<property file="ant-scripts/version.properties" />
	<target name="filter-config" description="Fills the setLamsConfiguration.sql file with the settings obtained from lams.properties">
	
		<copy overwrite="true" todir="${basedir}/sql-scripts" failonerror="true">
			
			<fileset dir="${basedir}/doc">
				<include name="setLamsConfiguration.sql" />
			</fileset>

			<filterset>
				<filter token="lams.version" value="${LAMS_VERSION}"/>
				<filter token="LANG_VERSION" value="${LANG_VERSION}"/>
				<filter token="SERVER_URL" value="${SERVER_URL}"/>
				<filter token="TEMP_DIR" value="${TEMP_DIR}"/>
				<filter token="DUMP_DIR" value="${DUMP_DIR}"/>
				<filter token="EAR_DIR" value="${EAR_DIR}"/>
				<filter token="server.version" value="${SERVER_VERSION}"/>
				<filter token="REPOSITORY_DIR" value="${REPOSITORY_DIR}"/>
				<filter token="LOCALE" value="${LOCALE}"/>
				<filter token="LOCALE_DIRECTION" value="${LOCALE_DIRECTION}"/>
				<filter token="WILDFIRE_DOMAIN" value="${WILDFIRE_DOMAIN}"/>
				<filter token="WILDFIRE_CONFERENCE" value="${WILDFIRE_CONFERENCE}"/>
				<filter token="WILDFIRE_USER" value="${WILDFIRE_USER}"/>
				<filter token="WILDFIRE_PASS" value="${WILDFIRE_PASS}"/>
				<filter token="LAMS_USER" value="${LAMS_USER}"/>
				<filter token="LAMS_PASS" value="${LAMS_PASS}"/>

			</filterset>
		</copy>
	</target>
</project> 
