<!-- 
	<form name = "forumForm">
			<field property="forum.title" depends="maxlength">
				<arg0 key="forum.label.title"/>
				<arg1 name="maxlength" key="${var:maxlength}" resource="false"/>
					<var>
						<var-name>maxlength</var-name>
						<var-value>50</var-value>
					</var>
			</field>
    </form>


	<form name = "messageForm">
			<field property="message.subject" depends="required, maxlength">
				<arg0 key="message.label.subject"/>
				<arg1 name="maxlength" key="${var:maxlength}" resource="false"/>
					<var>
						<var-name>maxlength</var-name>
						<var-value>50</var-value>
					</var>
			</field>
            <field property="message.body" depends="required">
				<arg0 key="message.label.body"/>
			</field>

		</form>

		-->